PORT=3000
CORS_ORIGIN=*
JWT_SECRET=change-me-to-a-real-secret
# Supabase project "xBilling". This one process needs access to all 5
# schemas, but they all live in the same Postgres instance, so one
# connection string covers every mounted domain's db/client.ts.
DATABASE_URL=postgresql://app_service:[YOUR-PASSWORD]@db.unkkskpfvrejjfgrjyab.supabase.co:5432/postgres
# Optional: enables live Claude-generated AI insight/copy in comms routes.
ANTHROPIC_API_KEY=
